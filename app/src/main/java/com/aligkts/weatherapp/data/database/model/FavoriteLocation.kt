package com.aligkts.weatherapp.data.database.model

data class FavoriteLocation(var id: Int = 0,
                            var latitude: Double = 0.0,
                            var longitude: Double = 0.0,
                            var date: Long = 0)